version: 2

sources:
  - name: globalmart
    database: raw
    schema: globalmart
    tables:
      - name: customer
      - name: orders
        columns:
          - name: orderid
            tests:
              - unique
              - not_null
      - name: product


# lowercase names is case insensitive
# if quotes are used, it is case sensitive
